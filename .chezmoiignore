# Files listed here are NOT applied on the matching machine.
# Add machine-only files inside the opposite block.

README.md

# OS-specific install scripts only run on their OS.
{{ if ne .chezmoi.os "darwin" }}
darwin-install-packages.sh
{{ end }}
{{ if ne .chezmoi.os "windows" }}
windows-install-packages.ps1
{{ end }}

{{ if ne .machine "macos-pdq" }}
# work-only files, e.g.:
# .config/work-tool/
{{ end }}

{{ if ne .machine "macos-personal" }}
# personal-only files, e.g.:
# .config/personal-only.conf
{{ end }}
